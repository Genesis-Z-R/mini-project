# Estudy Spring Boot Backend Configuration
spring.application.name=estudy-backend
server.port=8080

# ===================================================================
# MySQL DATABASE CONFIGURATION PLACEHOLDERS
# Populate these properties after manually installing & running MySQL
# ===================================================================
# spring.datasource.url=jdbc:mysql://localhost:3306/estudy_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=your_mysql_password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration Placeholders
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Security Secret Placeholder
estudy.app.jwtSecret=EstudySecretKeyForJWTSignatureGeneration32BytesLong!
estudy.app.jwtExpirationMs=86400000
